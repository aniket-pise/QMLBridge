/**
 * Represents alignment options for positioning elements
 * within a container.
 */
type Alignment = 'top' | 'left' | 'right' | 'bottom' | 'normal' | 'center' | 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right';

export default Alignment;
